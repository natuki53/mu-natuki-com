/** Public, fixed-schema server status snapshot served from the same origin. */
export const SERVER_STATUS_URL = '/api/server-status.json';

/** Refresh interval for the public snapshot. */
export const SERVER_STATUS_POLL_MS = 1_000;
